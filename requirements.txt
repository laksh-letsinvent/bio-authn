# Core
numpy>=1.26.0
scikit-learn>=1.4.0
pandas>=2.1.0
pillow>=10.0.0
requests>=2.31.0
pyyaml>=6.0.1
jsonschema>=4.21.0
tqdm>=4.66.0

# Face matching — ArcFace via ONNX (no TensorFlow)
insightface>=0.7.3
onnxruntime>=1.17.0
opencv-python>=4.5.5

# Demographic labeling — FairFace via PyTorch (no TensorFlow)
torch>=2.1.0
torchvision>=0.16.0
gdown>=5.1.0

# Corpus acquisition
datasets>=2.18.0
huggingface-hub>=0.21.0

# VLM adapters
anthropic>=0.34.0

# v2 will add: insightface (already here as ArcFace backend), onnxruntime (already here)
